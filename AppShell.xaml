<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:PSIB.Views"
       xmlns:products="clr-namespace:PSIB.Views.Products"
       xmlns:customers="clr-namespace:PSIB.Views.Customers"
       xmlns:vendors="clr-namespace:PSIB.Views.Vendors"
       xmlns:purchases="clr-namespace:PSIB.Views.Purchases"
       xmlns:sales="clr-namespace:PSIB.Views.Sales"
       xmlns:ap="clr-namespace:PSIB.Views.AccountPayables"
       xmlns:ar="clr-namespace:PSIB.Views.AccountReceivables"
       x:Class="PSIB.AppShell"
       Shell.FlyoutBehavior="Disabled"
       Title="PSIB 進銷存管理系統">

    <Shell.FlyoutHeader>
        <Grid BackgroundColor="#1E5F91" Padding="16" HeightRequest="80">
            <Label Text="PSIB 進銷存管理系統"
                   TextColor="White"
                   FontSize="18"
                   FontAttributes="Bold"
                   VerticalOptions="Center" />
        </Grid>
    </Shell.FlyoutHeader>

    <!-- 登入頁 (不顯示在 flyout) -->
    <ShellContent Route="Login"
                  ContentTemplate="{DataTemplate views:LoginPage}"
                  Shell.FlyoutItemIsVisible="False"
                  Shell.NavBarIsVisible="False" />

    <!-- 主選單 -->
    <FlyoutItem Title="首頁">
        <ShellContent ContentTemplate="{DataTemplate views:MainMenuPage}" Route="MainMenu" />
    </FlyoutItem>

    <!-- 基本資料 -->
    <FlyoutItem Title="商品管理">
        <ShellContent ContentTemplate="{DataTemplate products:ProductListPage}" Route="ProductList" />
    </FlyoutItem>
    <FlyoutItem Title="客戶管理">
        <ShellContent ContentTemplate="{DataTemplate customers:CustomerListPage}" Route="CustomerList" />
    </FlyoutItem>
    <FlyoutItem Title="廠商管理">
        <ShellContent ContentTemplate="{DataTemplate vendors:VendorListPage}" Route="VendorList" />
    </FlyoutItem>

    <!-- 進銷貨 -->
    <FlyoutItem Title="採購管理">
        <ShellContent ContentTemplate="{DataTemplate purchases:PurchaseListPage}" Route="PurchaseList" />
    </FlyoutItem>
    <FlyoutItem Title="銷售管理">
        <ShellContent ContentTemplate="{DataTemplate sales:SaleListPage}" Route="SaleList" />
    </FlyoutItem>

    <!-- 帳務 -->
    <FlyoutItem Title="應付帳款">
        <ShellContent ContentTemplate="{DataTemplate ap:AccountPayableListPage}" Route="AccountPayableList" />
    </FlyoutItem>
    <FlyoutItem Title="應收帳款">
        <ShellContent ContentTemplate="{DataTemplate ar:AccountReceivableListPage}" Route="AccountReceivableList" />
    </FlyoutItem>

</Shell>
